security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        app_user_provider:
            entity:
                class: App\Identity\Domain\Entity\UserEntity
                property: email

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            user_checker: App\Identity\Infrastructure\Security\UserAccountChecker
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
            logout:
                path: app_logout
                target: app_login
                enable_csrf: true
                csrf_parameter: _csrf_token
                csrf_token_id: logout

            # Activate different ways to authenticate:
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Note: Only the *first* matching rule is applied
    access_control:
        - { path: ^/(?:(?:en|fr|de)/)?contact$, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?auth/google/start$, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?auth/google/callback$, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?register$, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?verify-email, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?resend-verification$, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?forgot-password$, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?reset-password, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?login$, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?logout$, roles: PUBLIC_ACCESS }
        - { path: ^/(?:(?:en|fr|de)/)?admin, roles: ROLE_ADMIN }
        - { path: ^/, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
