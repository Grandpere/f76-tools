{% extends 'base.html.twig' %}

{% block title %}{{ 'progression.page_title'|trans }}{% endblock %}

{% block body %}
    {% set uiTranslations = {
        loadingPlayers: 'dashboard.state.loading_players'|trans,
        cannotLoadPlayers: 'dashboard.state.cannot_load_players'|trans,
        noPlayers: 'dashboard.state.no_players'|trans,
        playerNameRequired: 'dashboard.state.player_name_required'|trans,
        creatingPlayer: 'dashboard.state.creating_player'|trans,
        playerNameExists: 'dashboard.state.player_name_exists'|trans,
        createPlayerFailed: 'dashboard.state.create_player_failed'|trans,
        playerCreated: 'dashboard.state.player_created'|trans,
        exportDone: 'dashboard.state.export_done'|trans,
        exportFailed: 'dashboard.state.export_failed'|trans,
        importNoFile: 'dashboard.state.import_no_file'|trans,
        importInvalidFile: 'dashboard.state.import_invalid_file'|trans,
        importPreviewError: 'dashboard.state.import_preview_error'|trans,
        importPreviewUnknown: 'dashboard.state.import_preview_unknown'|trans,
        importPreviewUnknownDetailsPrefix: 'dashboard.state.import_preview_unknown_details_prefix'|trans,
        importConfirmPreview: 'dashboard.state.import_confirm_preview'|trans,
        importingProgress: 'dashboard.state.importing_progress'|trans,
        importDone: 'dashboard.state.import_done'|trans,
        importFailed: 'dashboard.state.import_failed'|trans,
        noSelectedPlayer: 'dashboard.state.no_selected_player'|trans,
        loadingItems: 'dashboard.state.loading_items'|trans,
        statsOverall: 'dashboard.stats.overall'|trans,
        statsMisc: 'dashboard.stats.misc'|trans,
        statsBook: 'dashboard.stats.book'|trans,
        statsByRank: 'dashboard.stats.by_rank'|trans,
        statsByList: 'dashboard.stats.by_list'|trans,
        statsLearnedOnTotal: 'dashboard.stats.learned_on_total'|trans,
        statsPercentSuffix: 'dashboard.stats.percent_suffix'|trans,
        statsRankPrefix: 'dashboard.stats.rank_prefix'|trans,
        statsListPrefix: 'dashboard.stats.list_prefix'|trans,
        bookCatalogLearned: 'progression.book_catalog_learned'|trans,
        bookCatalogPlans: 'progression.book_catalog_plans'|trans,
        bookCatalogRecipes: 'progression.book_catalog_recipes'|trans
    } %}
    <main class="catalog-page">
        {% include '_app_page_header.html.twig' with {
            localeSelectId: 'progression-header-locale-select',
            title: 'progression.title',
            subtitle: 'progression.description',
            activeNav: 'progression'
        } %}

        <section
            class="catalog-shell"
            data-controller="player-progression"
            data-player-progression-players-url-value="{{ apiPlayersUrl }}"
            data-player-progression-players-base-url-value="{{ apiPlayersBaseUrl }}"
            data-player-progression-initial-player-id-value="{{ activePlayerId ?? '' }}"
            data-player-progression-storage-key-value="{{ storageKey }}"
            data-player-progression-locale-value="{{ app.request.locale }}"
            data-player-progression-ui-translations-value="{{ uiTranslations|json_encode|e('html_attr') }}"
        >
            <section class="catalog-panel create-player-panel filters-panel">
                <h2>{{ 'progression.character_block_title'|trans }}</h2>
                <div class="catalog-toolbar progression-character-toolbar">
                    <label class="field create-player-field">
                        <span>{{ 'dashboard.create_player.name'|trans }}</span>
                        <div class="field-inline">
                            <input type="text" placeholder="{{ 'dashboard.create_player.placeholder'|trans }}" data-player-progression-target="createNameInput">
                            <button type="button" data-player-progression-target="createButton">{{ 'dashboard.create_player.submit'|trans }}</button>
                        </div>
                    </label>
                    <label class="field">
                        <span>{{ 'progression.player_list_label'|trans }}</span>
                        <select data-player-progression-target="playerSelect"></select>
                    </label>
                </div>
                {% set modsLink %}<a href="{{ path('app_dashboard', { _locale: app.request.locale }) }}">{{ 'progression.quick_link_mods'|trans }}</a>{% endset %}
                {% set minervaLink %}<a href="{{ path('app_minerva_rotation', { _locale: app.request.locale }) }}">{{ 'progression.quick_link_minerva'|trans }}</a>{% endset %}
                {% set booksLink %}<a href="{{ path('app_book_catalog', { _locale: app.request.locale }) }}">{{ 'progression.quick_link_books'|trans }}</a>{% endset %}
                <p class="catalog-note info-note">
                    <span class="info-note-content">
                        {{ 'dashboard.create_player.help'|trans }}
                        {{ 'progression.character_help_extended'|trans({'%modsLink%': modsLink, '%minervaLink%': minervaLink, '%booksLink%': booksLink})|raw }}
                    </span>
                </p>
                {% include '_catalog_state_line.html.twig' with { targetPrefix: 'player-progression' } %}
            </section>
            <section class="catalog-panel catalog-block progression-backup-panel">
                <h2>{{ 'progression.backup_title'|trans }}</h2>
                <div class="transfer-sections">
                    <article class="transfer-section">
                        <h3>{{ 'dashboard.transfer.import'|trans }}</h3>
                        <p class="catalog-note">{{ 'dashboard.transfer.import_help'|trans }}</p>
                        <p class="transfer-section-note">{{ 'dashboard.transfer.import_note'|trans }}</p>
                        <div class="transfer-controls">
                            <label class="transfer-file">
                                <input type="file" accept="application/json" data-player-progression-target="importFileInput">
                            </label>
                            <label class="transfer-checkbox">
                                <input type="checkbox" data-player-progression-target="importMergeCheckbox">
                                {{ 'dashboard.transfer.merge_mode'|trans }}
                            </label>
                            <button type="button" data-player-progression-target="importButton">{{ 'dashboard.transfer.import'|trans }}</button>
                        </div>
                        <p class="catalog-state" role="status" aria-live="polite" data-player-progression-target="backupState"></p>
                        <div class="transfer-unknown-panel" data-player-progression-target="importUnknownPanel"></div>
                    </article>
                    <article class="transfer-section">
                        <h3>{{ 'dashboard.transfer.export'|trans }}</h3>
                        <p class="catalog-note">{{ 'dashboard.transfer.export_help'|trans }}</p>
                        <p class="transfer-section-note">{{ 'dashboard.transfer.export_note'|trans }}</p>
                        <div class="transfer-actions">
                            <button type="button" data-player-progression-target="exportButton">{{ 'dashboard.transfer.export'|trans }}</button>
                        </div>
                    </article>
                </div>
            </section>
            <section class="catalog-panel catalog-block">
                <div data-player-progression-target="statsPanel"></div>
            </section>
        </section>
    </main>
{% endblock %}
