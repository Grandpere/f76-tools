{% extends 'base.html.twig' %}

{% block title %}{{ 'security.login.page_title'|trans }}{% endblock %}

{% block body %}
    <main class="auth-page">
        <section class="auth-card">
            {% include 'security/_card_header.html.twig' with { title: 'security.login.title' } %}

            {% include 'security/_flashes.html.twig' with { authError: error } %}

            <form method="post" class="auth-form">
                <label class="auth-field" for="username">
                    <span>{{ 'security.login.email'|trans }}</span>
                    <input
                        id="username"
                        name="_username"
                        type="email"
                        value="{{ last_username }}"
                        required
                        autofocus
                        autocomplete="username"
                    >
                </label>

                <label class="auth-field" for="password">
                    <span>{{ 'security.login.password'|trans }}</span>
                    <input
                        id="password"
                        name="_password"
                        type="password"
                        required
                        autocomplete="current-password"
                    >
                </label>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                <div class="auth-primary-actions">
                    <button type="submit">{{ 'security.login.submit'|trans }}</button>
                    <a class="auth-button-link" href="{{ path('app_register', { locale: app.request.locale }) }}">{{ 'security.login.to_register'|trans }}</a>
                </div>
            </form>

            {% include 'security/_links.html.twig' with {
                links: [
                    { route: 'app_resend_verification', label: 'security.login.to_resend' },
                    { route: 'app_forgot_password', label: 'security.login.to_forgot' },
                    { route: 'app_contact', label: 'security.login.to_contact' }
                ]
            } %}
        </section>
    </main>
{% endblock %}
