{% extends 'base.html.twig' %}

{% block title %}{{ 'security.forgot.page_title'|trans }}{% endblock %}

{% block body %}
    <main class="auth-page">
        <section class="auth-card">
            <p class="catalog-kicker">Fallout 76</p>
            <h1 class="auth-title">{{ 'security.forgot.title'|trans }}</h1>

            {% for message in app.flashes('success') %}
                <p class="auth-alert auth-alert-success">{{ message|trans }}</p>
            {% endfor %}
            {% for message in app.flashes('warning') %}
                <p class="auth-alert auth-alert-warning">{{ message|trans }}</p>
            {% endfor %}

            <form method="post" class="auth-form">
                <div class="visually-hidden" aria-hidden="true">
                    <label for="forgot-website">Website</label>
                    <input id="forgot-website" name="website" type="text" tabindex="-1" autocomplete="off">
                </div>
                <label class="auth-field" for="forgot-email">
                    <span>{{ 'security.forgot.email'|trans }}</span>
                    <input
                        id="forgot-email"
                        name="email"
                        type="email"
                        required
                        autocomplete="email"
                    >
                </label>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('forgot_password') }}">
                {% if captchaSiteKey %}
                    <div class="cf-turnstile" data-sitekey="{{ captchaSiteKey }}"></div>
                {% endif %}

                <button type="submit">{{ 'security.forgot.submit'|trans }}</button>
            </form>

            <p class="auth-links">
                <a href="{{ path('app_login', { locale: app.request.locale }) }}">{{ 'security.forgot.to_login'|trans }}</a>
                · <a href="{{ path('app_register', { locale: app.request.locale }) }}">{{ 'security.login.to_register'|trans }}</a>
                · <a href="{{ path('app_contact', { locale: app.request.locale }) }}">{{ 'security.login.to_contact'|trans }}</a>
            </p>
        </section>
    </main>
    {% if captchaSiteKey %}
        <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
    {% endif %}
{% endblock %}
